"""World engine — the orchestrator-side seam for the visual renderer.

`WorldManifest` declares what can be shown; `WorldDirector` translates bus
events into `SceneState`. See `docs/world_engine_design.md`.
"""
