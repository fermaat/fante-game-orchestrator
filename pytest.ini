[pytest]
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = -q -m "not integration"
markers =
    unit: unit tests
    functional: behavioural tests against real GameManager with fake adapters
    integration: integration tests requiring Ollama
    slow: slow tests
